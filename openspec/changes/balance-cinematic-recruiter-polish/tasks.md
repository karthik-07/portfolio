# Tasks

## 1. Lock the balanced surface and naming contract

- [x] 1.1 Add regression assertions requiring `WhatBroke?` in human-readable metadata, headings, prose, About copy, and terminal output while preserving lowercase `whatbroke` in commands, identifiers, repository paths, and URLs; run the targeted test first and verify it fails on the current spaced display name.
- [x] 1.2 Add regression assertions defining the major tilt set as flagship, all Experience cards, all supporting-project cards, all Skills cards, and Contact while excluding proof cells, nested demo panels, the About note, buttons, tags, pills, and navigation; run the targeted test first and verify it fails because Contact is not yet opted in.
- [x] 1.3 Add source assertions prohibiting broad green atmosphere on flagship and Contact while requiring neutral surfaces and localized semantic green accents; run the targeted test first and verify it fails on the current atmosphere rules.
- [x] 1.4 Reconcile the active `refine-interactive-skills` and `elevate-cosmic-presence` artifacts with this closed major-surface set and neutral-atmosphere decision; verify all active deltas describe compatible motion, ambience, and fallback behavior.

## 2. Standardize WhatBroke? and recruiter-facing copy

- [x] 2.1 Replace human-readable “What Broke?” or “WhatBroke” occurrences with `WhatBroke?` in structured metadata, section headings, flagship copy, About copy, and terminal responses without changing commands, DOM identifiers, script variables, repository paths, or URLs; verify the naming-context tests pass.
- [x] 2.2 Rewrite the flagship opening sentence to identify WhatBroke? as a local Linux forensics CLI that relates newly observed failures to preceding system changes before presenting its origin story; verify the existing status, technologies, proof points, demo, and GitHub link remain unchanged.
- [x] 2.3 Reframe the agentic Experience bullet so its recruiter-understandable development outcome leads and Graphify/Hermes appear as supporting implementation detail; verify no quantitative or production claim is added or removed.
- [x] 2.4 Refine Contact copy to reinforce availability for full-stack roles involving backend systems, AWS, and production ownership while retaining Canada and direct email actions; verify the copy remains concise at mobile widths.

## 3. Neutralize flagship and Contact atmosphere

- [x] 3.1 Remove or neutralize the broad green section-atmosphere washes behind flagship and Contact across dark and light themes; verify repository search and computed visual checks show no large green haze in either section.
- [x] 3.2 Restyle the flagship and Contact panel with shared neutral glass surface tokens, readable borders, and restrained depth; verify their text, demo, and controls maintain contrast in both themes.
- [x] 3.3 Retain green only on meaningful active-build, availability, action, status-light, thin-border, and edge-signal details; verify green no longer fills a large section background or dominates the content surface.

## 4. Complete the major-card 3D system

- [x] 4.1 Opt the Contact panel into the existing shared tilt coordinator and verify no new pointer listener, per-component handler, animation loop, or dependency is introduced.
- [x] 4.2 Add one shared low-opacity pointer-relative lighting treatment to major tilt surfaces using the coordinator's existing pointer variables; verify the highlight stays inside the active surface and nested controls do not transform independently.
- [x] 4.3 Keep proof points, project-proof cells, nested WhatBroke? panels, the About note, tags, pills, buttons, terminal controls, and navigation spatially static; verify the closed-set structural assertions pass.
- [x] 4.4 Preserve static transforms for reduced motion, constrained/static tiers, coarse pointers, touch-only input, and no-JavaScript use while retaining focus, selection, actions, and readable content; verify existing and new fallback assertions pass.

## 5. Visual and interaction acceptance

- [x] 5.1 Verify dark and light themes at 1440px with a fine pointer: major surfaces share bounded tilt/lift/lighting, only the hovered surface responds, nested content remains stable, and flagship/Contact backgrounds stay neutral.
- [x] 5.2 Verify tablet and 320px/430px mobile layouts in both themes: surfaces remain static where pointer precision is unavailable, copy does not clip, controls remain reachable, and no horizontal page overflow appears.
- [x] 5.3 Verify keyboard-only, reduced-motion, no-JavaScript, canvas-blocked, and GSAP-blocked states: all recruiter-critical content remains visible and usable without tilt, lighting, or ambience.
- [x] 5.4 Review the complete page as a recruiter scan and verify the hero, WhatBroke?, Experience, supporting projects, Skills, About, and Contact preserve clear hierarchy without additional celestial decoration or competing green haze.

## 6. Final validation

- [x] 6.1 Run `node --check script.js`, the complete structural test suite, duplicate-ID and internal-anchor audits, and `git diff --check`; verify every command passes with no new runtime dependency.
- [x] 6.2 Run strict OpenSpec validation for `balance-cinematic-recruiter-polish` and every reconciled active change; verify the specifications contain no conflicting naming, surface, ambience, or motion requirements.
